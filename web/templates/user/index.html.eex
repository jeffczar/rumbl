<h1>User List</h1>

<table class="table">
  <%= for user <- @users do %>
      <tr>
        <td><%= user.name %> (<b><%= user.id %></b>)</td>
        <td><%= link "View", to: user_path(@conn, :show, user) %></td>
      </tr>
  <% end %>
</table>
